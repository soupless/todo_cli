# Command structure
```
todo
    group
        create --id <str> --path  <str>
        switch [--id] <str>
        remove [--id] <str>
    task
        add
        delete
        update
        edit
        list [--verbose | --quiet]
```
